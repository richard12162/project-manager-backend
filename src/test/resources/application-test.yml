spring:
  jpa:
    hibernate:
      ddl-auto: validate
    open-in-view: false

  flyway:
    enabled: true

app:
  jwt:
    secret: "change-this-dev-secret-change-this-dev-secret-change-this-dev-secret"
    expiration-ms: 3600000